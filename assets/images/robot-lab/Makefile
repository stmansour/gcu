# ─────────────────────────────────────────────────────────────────────────────
# assets/images/robot-lab — shared Robot Lab images (currently empty)
# ─────────────────────────────────────────────────────────────────────────────

REPO_ROOT := $(shell git rev-parse --show-toplevel)
DIST      := $(REPO_ROOT)/dist
THISDIR   := assets/images/robot-lab

.PHONY: all clean package

all:
	@echo "*** $(THISDIR): nothing to build ***"

clean:
	@echo "*** $(THISDIR): nothing to clean ***"

package:
	@echo "*** $(THISDIR): nothing to package ***"
